[project]
name = "review-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "grpcio>=1.71.0",
    "grpcio-tools>=1.71.0",
    "psycopg[binary]>=3.2.7",
    "python-dotenv>=1.1.0",
]
